import Image from "next/image";

export default function LaunchPage() {
    return (
        <div className="flex flex-col gap-2">
            <h1>launch token page.</h1>
            <h1>we need a form for name, symbol, img, etc</h1>
        </div>
    );
}